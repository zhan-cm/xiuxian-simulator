[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "xiuxian-simulator"
version = "0.27.0"
description = "《问道长生》规则驱动的本地修仙文字模拟器 V0.27 状态反馈与新手提示版"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
xiuxian = "xiuxian_simulator.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
