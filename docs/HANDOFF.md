# 《永恒之道》跨设备接续与项目交接全景指南

> 本指南用于在更换电脑或在新设备上使用 Google Antigravity 时，实现 **0 认知损耗、0 进度丢失、一键无缝接续**。

---

## 一、项目核心全景与分支状态

- **项目名称**：《永恒之道》（原《问道长生》，高自由度本地规则驱动修仙文字模拟器）
- **远程仓库**：`https://github.com/zhan-cm/xiuxian-simulator.git`
- **核心主分支**：`main`（已全量快进合并所有沉浸式 V2 成果，包含 22 个核心特性提交，与 `codex/immersive-ui` 保持完全一致）
- **当前版本基准**：`v1.0.0` / 沉浸式 V2 水墨动漫架构
- **核心开发准则**：
  - **路径限定**：全量代码与资源操作严格限定在项目工作区内部，禁止跨越系统盘或其他不相关目录；
  - **既定协议**：**「方式 A（自动推送）」**——每次功能修改必须经过全栈测试（Vitest 48 项 + Pytest 274 项 + 前端生产打包 + 资源校验）全部 100% 绿灯后，方可提交并推送至远端分支。

---

## 二、新设备极速部署开箱（约 3 分钟）

在全新电脑上拉取工程后，按以下步骤初始化环境：

### 1. 克隆代码仓库
```bash
git clone https://github.com/zhan-cm/xiuxian-simulator.git
cd xiuxian-simulator
git checkout main
```

### 2. 初始化 Python 后端环境
```powershell
# 建议 Python 3.11 或 3.12
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\pip install -e .[dev]
```

### 3. 初始化前端依赖与构建
```powershell
cd frontend
npm install
npm run build
cd ..
```

### 4. 验证全栈测试基准（确保 100% 绿灯）
```powershell
# 1. 前端 Vitest 48 项测试
npm --prefix frontend test -- --run

# 2. 前端生产打包
npm --prefix frontend run build

# 3. 后端 Pytest 274 项测试
.\.venv\Scripts\python.exe -m pytest

# 4. 发布资产完整性校验
.\.venv\Scripts\python.exe scripts\verify_release.py
```

### 5. 一键启动游戏
- Windows 下直接双击根目录 `启动游戏.bat`（或 `start_game.bat`）即可同时启动前后端并呼出沉浸式桌面客户端。

---

## 三、已完成的核心系统与视觉资产清单

1. **沉浸式水墨宣纸主题与视窗架构**：
   - 支持 Windows 下独立桌面窗口启动；
   - 水墨序幕与两重创角大殿堂（命格 / 道体 / 加点平衡 / 纯随机天赋卡片 / 随时再入轮回）；
   - 仙途指津系统、仙道十重天境界通天图与大境界突破祭坛；
   - 行动台分类可选卡片（潜修 / 历练 / 生财 / 红尘）。

2. **九州大舆图 2D 全景自然地界系统**：
   - 彻底消弭死板井字网格，以蜿蜒奔腾的「通天大江」、「沧溟龙峡」与「天脊龙脉」、「昆仑西岭」、「十万祖山」自然褶皱划分九州地界；
   - 各州独立板块光标悬浮动态立体浮起（`translateY(-4px)`）与金墨光晕；
   - 100% 满屏全宽长卷 + 舒展三栏风物案卷（灵域概览 / 灵脉虚实 / 临机行止），文字绝不出界。

3. **仙家洞府实体建筑与真水墨山水长卷**：
   - 背景引入青绿水墨《仙家洞天胜境》长卷，带有灵雾呼吸浮动动效；
   - 六大设施专属古典实体建筑图腾（丹房玄火宝鼎、灵田青玉叠翠、器坊玄铁重砧、静室云崖悟道亭、聚灵阵八卦阵盘、禁制镇山神篆）。

4. **2D 动漫精美人物立绘与双模二次元头像引擎**：
   - 核心人物「顾清玄」实装高精 2D 动漫立绘原画（`frontend/src/assets/avatars/gu_qingxuan.jpg`），具备呼吸悬浮放大与光影漫反射图层；
   - 全服同道全面升级为细腻的 2D 动漫矢量肖像（大眼灵眸、多层渐变虹膜、睫毛微翘、少女腮红、动感发丝、天使光环）；
   - 右下角朱砂篆字小印，结合「相貌风仪」专栏动态生成古典诗意风采描述。

---

## 四、新电脑 Antigravity 一键接续提示词（直接复制发送）

在新电脑上使用 Antigravity 打开项目工作区后，直接将以下框内文本作为第一条消息发送给 AI 助手：

```markdown
# 《永恒之道》新设备接续开发启动

你好！这是我们在之前设备上完成的《永恒之道》（修仙文字模拟器）项目，已全量同步至 GitHub。现在我们在新电脑上继续推进，请查阅以下状态并做好准备：

### 一、工程环境与分支状态
- 项目名称：《永恒之道》（修仙文字模拟器）
- 当前分支：main（已与远端完全同步，包含最新全部 22 个沉浸式 V2 提交）
- 既定协议：「方式 A（自动推送）」——每次功能修改并经全栈测试（Vitest 48 项 + Pytest 274 项 + 前端生产打包）全部 100% 通过后，自动提交并推送至远端。
- 详尽交接文档已在项目内的 docs/HANDOFF.md。

### 二、已就绪的核心资产与技术架构
1. 沉浸式水墨宣纸主题独立视窗框架；
2. 九州大舆图 2D 全景自然地界系统（自然山川水系划分、板块悬浮立体光晕、舒展三栏风物案卷）；
3. 仙家洞府青绿水墨长卷与六大设施专属建筑图腾；
4. 2D 动漫人物立绘与双模二次元头像渲染引擎（顾清玄已实装高清 2D 动漫原画大图，其余同道为精美 2D 动漫矢量肖像）；
5. 全栈测试基准：Vitest 48 项测试全绿，Pytest 274 项测试全绿，前端打包与发布资产校验通过。

### 三、当前待推进优先级任务
1. 继续为其他 5 位核心人物定制深化专属 2D 动漫立绘原画（白凝霜、云栖、谢无咎、墨尘、洛浅浅）；
2. 推进 Visual Novel 风格立绘对话交互框、剧情推演事件或界面交互的沉浸式打磨。

请先核验本地环境与测试状态，准备好后告诉我我们可以从哪个人物或模块开始！
```

---

## 五、后续任务推进路线详述

### 1. 核心人物 2D 动漫立绘队列（待推进）
| 人物名 | 身份设定 | 外貌风仪与视觉特色 | 目标落位文件 |
| :--- | :--- | :--- | :--- |
| **白凝霜** | 北原雪族圣女 · 雪岭玄女 | 三千银丝如瀑、冰蓝星眸、轻若蝉翼霜白羽裳、周身极寒雪晶微芒 | `frontend/src/assets/avatars/bai_ningshuang.jpg` |
| **云栖** | 天机坊市老板娘 · 天机灵贾 | 流云飞天髻、金雀步摇、绯红流金轻罗羽衣、眼波流转、精明聪慧 | `frontend/src/assets/avatars/yun_qi.jpg` |
| **谢无咎** | 血魔宗少主 · 血煞魔君 | 乌发披肩、剑眉斜飞入鬓、眉心幽血魔纹、玄黑织金战袍暗蕴凶戾 | `frontend/src/assets/avatars/xie_wujiu.jpg` |
| **墨尘** | 古妖山少主 · 古妖少主 | 发间微翘墨玉龙角、璀璨金瞳、衣襟微敞玄骨链坠、桀骜少年妖修 | `frontend/src/assets/avatars/mo_chen.jpg` |
| **洛浅浅** | 合欢宗妙姝 · 合欢妙姝 | 青丝彩缎银铃脆响、眼尾胭脂、绯粉罗裙挽臂、娇媚入骨乱人道心 | `frontend/src/assets/avatars/luo_qianqian.jpg` |

### 2. 视觉小说（Visual Novel）式立绘对话交互演出
- 人际对话、坊市偶遇、同门切磋、探秘探索时，呼出带有大立绘差分半身像、专属台词气泡与交互按钮（切磋/赠礼/论道/结缘）的沉浸式案卷。

### 3. 洞府设施动态推演
- 丹房开炉炼丹、灵田浇灌成熟、器坊开炉铸器、聚灵阵聚气吐纳的动态推演与产出结算面板。
